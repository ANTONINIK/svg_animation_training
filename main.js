function changeBlackOutVisible() {
  console.dir(logo__left);
  // blackout.style.opacity = 0;

  logo__left.style.opacity = 1;
  logo__left.style.transform = "translateX(20px) scale(1)";
}

changeBlackOutVisible();
